<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Devour!{{else}}Re-Order!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Remove From History</button>
</li>
